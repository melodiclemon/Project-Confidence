///card_ofType(type)

// Checks if the current card is of a certain type.

var i;

for(i = 0; i < array_length_1d(target.immuneToCardType); i++)
{
    if(type == argument0)
    {
        return true;
    }   
}
return false;
